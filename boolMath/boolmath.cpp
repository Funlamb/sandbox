#include <iostream>
#include <string>
using namespace std;

int main() {
        // char a;
        // cin >> a;
        // bool isTrue;
        // if (a >= 'a' && a <= 'z' || a >= 'A' && a <= 'Z') {
        //         isTrue = true;
        // } else {
        //         isTrue = false;
        // }
        // cout << isTrue;
        int a = -1,  d = -1,  b = -1;
        a >= 5 ? d = 30 : b = 12;
        cout << a << " " <<  d << " " << b;
        return 0;
}
